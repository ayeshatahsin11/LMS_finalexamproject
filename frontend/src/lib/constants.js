// Course levels - fixed, unlike categories (which moved to the database
// and are admin-managed via /admin/categories, see lib/useCategories.js).

export const LEVELS = ["beginner", "intermediate", "advanced"];